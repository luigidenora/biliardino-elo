# Attiva la modalità sviluppo con mock data e dev toolbar
VITE_DEV_MODE=true

# API base URL per il dev server
VITE_API_BASE_URL=/api

# Token admin opzionale per dev toolbar (broadcast e clear confirmations)
# Se non impostato qui, puoi inserirlo tramite il login dialog del broadcast admin
# VITE_ADMIN_TOKEN=
